{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd client && npm ci && npm run build && cd ../server && npm ci && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npm start"
  }
}
